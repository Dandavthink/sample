const config = {
    mySqlDbConnection: {
        host: 'localhost',
        port: 3306,
        database: 'vdrive',
        user: 'root',
        password:'password',
        multipleStatements: true
    }
}

module.exports = config;