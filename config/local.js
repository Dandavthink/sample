const config = {
    mySqlDbConnection: {
        host: 'local',
        port: 3306,
        database: 'vdrive',
        user: 'root',
        multipleStatements: true
    }
}

module.exports = config;